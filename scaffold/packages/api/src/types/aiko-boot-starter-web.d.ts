declare module '@ai-partner-x/aiko-boot-starter-web' {
  /**
   * aiko-boot-starter-web is a workspace dependency without published TS types.
   * We only need the `getExpressApp()` export for this project.
   */
  export function getExpressApp(): any;
}

